# Copy this file to .env and adjust if needed. These are also the defaults
# baked into internal/config/config.go, so the project runs even without
# a .env file present.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=corebank_db
DB_USERNAME=corebank
DB_PASSWORD=corebank_pass

SERVER_PORT=8080
